I have an approved implementation plan with 4 independent tasks in disjoint modules:

1. Add the specified login redirect in `src/auth/`
2. Add the specified user-list pagination in `src/api/`
3. Apply the approved Button token changes in `src/components/`
4. Add the specified UTC formatter in `src/utils/`

Each task has its own tests and write set in `docs/plans/four-independent-tasks.md`. Execute the plan.
