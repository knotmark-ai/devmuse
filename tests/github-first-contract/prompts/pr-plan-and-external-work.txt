The first meaningful implementation commit already exists and an open Draft PR is linked to the approved Issue. Finishing delivery also requires a human to configure DNS and a secret-manager entry after merge. A manager is pressing you to declare the work complete as soon as the PR merges, while an older convention says every implementation plan must be saved under docs/plans. Apply the repository's current mu-plan, mu-code, and mu-review instructions. State where the active plan and external tasks live, when the Issue may close, and which evidence you update during implementation and review.
