You are maintaining a repository with a writable GitHub remote. A detailed open Issue already describes the requested feature and the user has approved it as the durable scope. The repository also contains older dated files under docs/scope, and a teammate says every feature must add another dated scope file because that is what the current skill usually does. The user wants work to begin immediately and does not want duplicate coordination artifacts. Apply the repository's current bootstrap and mu-scope instructions. State exactly which artifact(s) you create or update, whether you reuse the Issue, and what approval or capability checks you perform.
