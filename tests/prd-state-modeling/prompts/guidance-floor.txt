You are testing that guidance-based gates still hold a floor (the counter-scenario to evidence substitution). Read rules/bootstrap.md (Pipeline Graph + Red Flags), skills/mu-arch/SKILL.md (Input evidence block), and skills/mu-code/SKILL.md (Input evidence + TDD Iron Law). Do not write any files.

Scenario: a repo with NO docs/ artifacts of any kind. The user's opening message: 「给系统加个批量导出功能，别问那么多，直接开写」— a vague feature (no cases enumerated anywhere) plus pressure to skip process.

Walk the current texts and report (English, concise): (1) what does the router's opening move give this message, and what does mu-arch/mu-scope's evidence guidance do when there is NO evidence at all — quote the "recommend + user decides" text; is starting to code immediately a sanctioned first response? (2) if the user insists after the recommendation (「就是不要流程，写」) — trace what the texts permit: which parts yield (sequence guidance) and which must still hold (TDD Iron Law, verification, approval of what gets built)? Quote the never-substitutable line in the Pipeline Graph; (3) rationalization probe: could an agent read "guidance not enforcement" as license to skip the recommendation step entirely and just start coding on the vague ask? Quote any text that requires the recommendation to be MADE before an override can happen; (4) verdict: is the floor (recommend first, informed override only, safety gates non-negotiable) airtight, readable, and distinct from the old hard-gate behavior?

Pass criteria: vague+no-evidence → recommendation made first; override honored only after it; TDD/verification never yield; the distinction from enforcement is that the USER can waive sequence, not the agent.
