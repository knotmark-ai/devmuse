interface:
  display_name: "DevMuse Grill"
  short_description: "Stress-test a plan or design"
  default_prompt: "Use $mu-grill to stress-test this plan until its risky decisions are resolved."
policy:
  allow_implicit_invocation: false
