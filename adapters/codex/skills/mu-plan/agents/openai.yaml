interface:
  display_name: "DevMuse Plan"
  short_description: "Write a traceable implementation plan"
  default_prompt: "Use $mu-plan to write a durable UC-traceable implementation plan."
policy:
  allow_implicit_invocation: false
