interface:
  display_name: "DevMuse Domain Model"
  short_description: "Create or update the domain model"
  default_prompt: "Use $mu-model to create or update the repository domain model."
policy:
  allow_implicit_invocation: false
