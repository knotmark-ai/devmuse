# Retro: 2026-07-02 → 2026-07-16 (14d)

> Window: 2026-07-02T00:00:00 → 2026-07-16
> Generated by /mu-retro

## Metrics

| Metric | Value |
|---|---|
| Commits | 14 |
| Contributors | 1 (Jeff Yu) |
| Lines changed | +2457 / −3804 (**net −1347**) |
| Test files | 28 |
| Hottest files | `rules/bootstrap.md` (4), `README.md` (3), `docs/architecture.md` (3) |

Per-author: Jeff Yu — 14 commits, top area: meta-files (rules / README / architecture docs). No business skill in the top-3 — a pure infrastructure period.

**Wiki freshness:** ✅ fresh — last updated 2026-07-16, 0 commits behind (first live run of the new check).

## Theme

The mattpocock/skills absorption campaign, end to end: skill-quality lexicon → on-demand skills user-invoked → CONTEXT.md domain glossary (mechanism + dogfood) → mu-write-skill slimmed by its own checklist → grilling primitive + /mu-grill → mu-debug red-loop-first → doc sync contract (CLAUDE.md) + CN twins → 1.1.0 + full wiki rebuild + bootstrap slimmed.

## Reflection

**What went best** (Jeff): establishing the skill-quality bar first, then turning it on our own skills. The order was causal — the lexicon landed first and every later cut (mu-write-skill 477→191, bootstrap 169→89) was justified by it rather than by taste; RED/GREEN subagent verification made deletions safe instead of hopeful.

**Most surprising** (agent, delegated):
1. RED baselines repeatedly showed strong models already do the "obvious" disciplines by default (one question at a time, look up facts yourself). The real gaps were always the **stopping rules** — an invented "max 3 questions" cap, a hypothesis-shaped repro. Skill text earns its tokens at the edges, not the middle.
2. The stalest doc was the one recording staleness: `_overview.md`'s Doc-vs-Code-Conflicts entry outlived the fix it described (`304043d`), and a verification subagent trusted the record over the code — reporting a "mu-design straggler" that no longer existed. Doc drift doesn't just mislead; it manufactures phantom bugs.
3. Shipping five new mechanisms at net −1347 lines.

**Change next period** (agent, delegated):
1. Verify subagent claims against current code before acting on them (the phantom-straggler lesson).
2. Run `/reload-plugins` after editing any skill — the plugin cache served a stale mu-retro during this very retro; an unreloaded edit is an undeployed edit.
3. Keep audit-on-touch (per CLAUDE.md) for the remaining long skills (mu-code 600+ lines); no bulk sweep.

## Learnings captured to memory

- Subagent reports can echo stale docs as live findings — verify against code first.
- Plugin cache lag: skill edits are invisible in-session until `/reload-plugins`.
